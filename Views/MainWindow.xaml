<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="UBB_SE_2025_EUROTRUCKERS.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="ms-appx:///Assets/background.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation bar con fondo semitransparente -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="#7F000000">
            <Button Content="Deliveries" Command="{Binding NavigateToDeliveriesCommand}" 
                       Style="{StaticResource NavigationButtonStyle}"/>
            <Button Content="Log Out" Command="{Binding LogOutCommand}"
                       Style="{StaticResource NavigationButtonStyle}"/>
        </StackPanel>

        <!-- Dynamic content con fondo semitransparente -->
        <Frame x:Name="MainContent" Grid.Row="1" Background="#7FFFFFFF"/>
    </Grid>
</Window>
